---
"@klappay/one": patch
---

Fix the `black` and `white` variants' logo mark rendering visibly stretched horizontally. Their source assets were generated by resizing the embedded raster in isolation instead of the SVG's own square canvas, producing a non-square (40x35) image that the button's square `width`/`height` CSS then stretched to fit.
